package org.example;

public enum BondType {
    ROR, // Roczne, zmienne (NBP)
    DOR, // Dwuletnie, zmienne (NBP)
    TOS, // Trzyletnie, stałe
    COI, // Czteroletnie, indeksowane inflacją
    EDO, // Dziesięcioletnie, indeksowane inflacją (kapitalizacja)
    ROS, // Rodzinne 6-letnie
    ROD  // Rodzinne 12-letnie
}